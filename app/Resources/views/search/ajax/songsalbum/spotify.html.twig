<div class="col-xs-10 offset-xs-1 message">
    Cliquer sur une musique pour la lancer
</div>
<ul class="col-xs-12">
    {% for song in songs %}
        <li class="container">
            <div class="col-xs-1">
                <span class="add-to-playlist" data-type="spotify" data-name="Musique n°1" data-id="123456">
                    +
                </span>
            </div>
            <div class="col-xs-1">
                <span data-song-id="{{ song.id }}" data-platform="spotify" class="playTrack">
                    ►
                </span>
            </div>
            <div class="col-xs-10">
                {{ song.name }}
            </div>
        </li>
    {% endfor %}
</ul>