<div class="col-xs-10 offset-xs-1">
    {% for song in songs %}
        <div class="item {% if loop.index is odd %}odd{% endif %}">
            <div class="song">
                {{ song.track.name }}
            </div>
            <div class="artist">
                {{ song.track.artists[0].name }}
            </div>
        </div>
    {% else %}
        <div>
            Aucun son trouvé dans cette playlist
        </div>
    {% endfor %}
</div>
